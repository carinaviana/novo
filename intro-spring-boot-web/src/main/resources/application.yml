server:

 port: 8090
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dbspotmusic?useTimezone=true&serverTimezone=UTC&createDatabaseIfNotExist=true
    username: root
    password: admin

  jpa:
    properties:
      hibernate:
        show_sql: true
        hbm2ddl:
          auto: update
